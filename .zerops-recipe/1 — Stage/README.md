<!-- #ZEROPS_EXTRACT_START:intro# -->
A single-node stage: one container that can scale *vertically* (1–8 GB RAM) for
bigger operation counts, but stays at one container because the Postgres engine is
in-process. Good for repeatable benchmark runs before sharing a public URL.
<!-- #ZEROPS_EXTRACT_END:intro# -->
