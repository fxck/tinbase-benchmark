<!-- #ZEROPS_EXTRACT_START:intro# -->
The full three-service stack at single-node scale — managed Postgres, one tinbase
backend, one dashboard, both subdomain-exposed. The durable, shareable step before
production; same wiring as prod, smaller footprint.
<!-- #ZEROPS_EXTRACT_END:intro# -->
